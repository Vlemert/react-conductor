{
  "name": "react-conductor",
  "version": "0.1.0",
  "description": "React renderer for Electron",
  "main": "index.js",
  "author": "Niels Vleeming",
  "license": "MIT",
  "scripts": {
    "test": "(cd ./packages/react-conductor && yarn test)",
    "test:watch": "(cd ./packages/react-conductor && yarn test:watch)",
    "start": "(cd ./packages/example && yarn start)"
  },
  "devDependencies": {
    "prettier": "^1.7.4"
  },
  "dependencies": {},
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
